/**********************************************************
 * Desc: generated by TblTool
 * Xml : ActorInfo.xml
 **********************************************************/

syntax = "proto3";

import "xrescode_extensions_v3.proto";

package FaGame;

// excel: ../ActorInfo.xlsx
// sheet: ActorInfo
message FaActorInfo {
    sint32 id         = 1; // Id
    sint32 max_hp     = 2; // MaxHP
    sint32 move_range = 3; // MoveRange
    sint32 attack     = 4; // Attack
};
message FaActorInfo_Tbl {
    option (xrescode.file_path) = "ActorInfo.bytes";
    // option (xrescode.file_list)       = "ActorInfo.files";
    option (xrescode.indexes) = {
        fields : "id"
        index_type : EN_INDEX_KV
    };
    option (xrescode.tags) = "client";
    option (xrescode.tags) = "server";

    sint64   crc32             = 1;                                 // excel crc32
    repeated FaActorInfo items = 6 [ (xrescode.excel_row) = true ]; // table data array
};
